export interface Destacados {
    id: number; //ID <- Tipo number para que pueda ser auto incremental
    img: string;
    alt: string;
    precio: number;
    tituloLibro: string;
    autor: string;
}
